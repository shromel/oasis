**LevelPill** — a tappable pill in the Train level selector. Active gets the gold wash; the user's *current* level carries a palm-green dot.

```jsx
<LevelPill level={1} name="Foundation" current active />
<LevelPill level={2} name="Strength" onClick={…} />
```
